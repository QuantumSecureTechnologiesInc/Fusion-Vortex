[package]
name = "fusion-ledger"
version = "1.0.0"
edition = "2021"
authors = ["Fusion Language Team"]
license = "MIT OR Apache-2.0"
description = "Deterministic, append-only execution ledger with crash-safe replay"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
