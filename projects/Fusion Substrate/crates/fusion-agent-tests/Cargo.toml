[package]
name = "fusion-agent-tests"
version = "1.0.0"
edition = "2021"
authors = ["Fusion Language Team"]
license = "MIT OR Apache-2.0"
description = "Integration tests for Phase 2 agent execution layer"

[dependencies]
fusion-agent-spec = { path = "../fusion-agent-spec" }
fusion-agent-graph = { path = "../fusion-agent-graph" }
fusion-agent-runtime = { path = "../fusion-agent-runtime" }
fusion-runtime = { path = "../fusion-runtime" }
fusion-ledger = { path = "../fusion-ledger" }
fusion-policy = { path = "../fusion-policy" }
fusion-mcp-spec = { path = "../fusion-mcp-spec" }
serde_json = "1.0"
tempfile = "3.8"
