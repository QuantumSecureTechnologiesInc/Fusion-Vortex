[package]
name = "fusion-runtime"
version = "1.0.0"
edition = "2021"
authors = ["Fusion Language Team"]
license = "MIT OR Apache-2.0"
description = "Crash-only runtime with deterministic replay - restart equals deterministic state"

[dependencies]
fusion-mcp-spec = { path = "../fusion-mcp-spec" }
fusion-ledger = { path = "../fusion-ledger" }
fusion-policy = { path = "../fusion-policy" }
serde_json = "1.0"
fusion_std = "1.0.0"
