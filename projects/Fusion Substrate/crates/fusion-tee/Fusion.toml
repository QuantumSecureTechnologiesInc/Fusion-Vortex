[package]
name = "fusion-tee"
version = "1.0.0"
edition = "2021"
authors = ["Fusion Language Team"]
license = "MIT OR Apache-2.0"
description = "Trusted Execution Environment abstraction with remote attestation"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
sha3 = "0.10"
hex = "0.4"
fusion_std = "1.0.0"
