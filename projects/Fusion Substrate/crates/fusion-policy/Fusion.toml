[package]
name = "fusion-policy"
version = "1.0.0"
edition = "2021"
authors = ["Fusion Language Team"]
license = "MIT OR Apache-2.0"
description = "Exhaustive policy enforcement with zero implicit permissions"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
fusion_std = "1.0.0"
