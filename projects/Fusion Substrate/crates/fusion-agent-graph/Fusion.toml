[package]
name = "fusion-agent-graph"
version = "1.0.0"
edition = "2021"
authors = ["Fusion Language Team"]
license = "MIT OR Apache-2.0"
description = "Agent execution graph with state machine and cursor-based partial execution"

[dependencies]
fusion-agent-spec = { path = "../fusion-agent-spec" }
serde = { version = "1.0", features = ["derive"] }
fusion_std = "1.0.0"
