[package]
name = "fusion-agent-spec"
version = "1.0.0"
edition = "2021"
authors = ["Fusion Language Team"]
license = "MIT OR Apache-2.0"
description = "Locked agent execution specification - the ABI of agents"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
