[package]
name = "fusion-mcp-spec"
version = "1.0.0"
edition = "2021"
authors = ["Fusion Language Team"]
license = "MIT OR Apache-2.0"
description = "Locked MCP 1.0 protocol specification with backward compatibility guarantee"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
fusion_std = "1.0.0"
