[package]
name = "reactor-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive", "env"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
colored = "2.0"
inquire = "0.6"
lazy_static = "1.4"
fusion_std = "1.0.0"

[target.'cfg(unix)'.dependencies]
# Unix-specific dependencies for IPC

[features]
default = []
nebula_2_1 = []
supernova = []
