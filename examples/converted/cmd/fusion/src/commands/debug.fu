// SOURCE: cmd/fusion/src/commands/debug.rs
// NOTE: Converted into FSN mirror under fsn/converted.
use anyhow::Result;
use fusion_debugger;

fn debug(target: Option<&str>) -> Result<()> {
    fusion_debugger::start(target)
}
