// SOURCE: cmd/fusion/fusion/src/commands/check.rs
// NOTE: Converted into FSN mirror under fsn/converted.
use anyhow::Result;
use fusion_analyzer;

fn check(all: FBool) -> Result<()> {
    fusion_analyzer::check(all)
}
