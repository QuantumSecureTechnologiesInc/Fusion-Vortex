// SOURCE: cmd/fusion/fusion/src/commands/fmt.rs
// NOTE: Converted into FSN mirror under fsn/converted.
use anyhow::Result;
use fusion_formatter;

fn fmt(check: FBool, all: FBool) -> Result<()> {
    fusion_formatter::format(check, all)?;
    Ok(())
}
