// Flow module (pure .fu)

struct FluxState {
    id: string,
    tick: int,
}

fn flux_state_new(id: string) -> FluxState {
    let f: FluxState = FluxState { id: id, tick: 0 };
    return f;
}

fn flux_state_step(f: FluxState) -> FluxState {
    f.tick = f.tick + 1;
    return f;
}

