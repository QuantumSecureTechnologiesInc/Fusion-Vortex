[package]
name = "fusion-agent-runtime"
version = "1.0.0"
edition = "2021"
authors = ["Fusion Language Team"]
license = "MIT OR Apache-2.0"
description = "Agent runtime bridge connecting agent graphs to Phase 1 crash-only runtime"

[dependencies]
fusion-agent-spec = { path = "../fusion-agent-spec" }
fusion-agent-graph = { path = "../fusion-agent-graph" }
fusion-runtime = { path = "../fusion-runtime" }
fusion-mcp-spec = { path = "../fusion-mcp-spec" }
fusion-policy = { path = "../fusion-policy" }
serde_json = "1.0"
