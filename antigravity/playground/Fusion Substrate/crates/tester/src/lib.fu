use anyhow::Result;
struct TestResult {
    pub passed: FSize,
    pub failed: FSize,
    pub total: FSize,
}
fn run_tests(_filter: Option<&str>, _release: FBool, _bench: FBool) -> Result<()> {
    println!("Running tests...");
    Ok(())
}
