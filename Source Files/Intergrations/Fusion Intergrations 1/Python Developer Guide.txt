Developer Guide: Fusion for PythonThis guide explains how to integrate the Fusion Hybrid Engine into your Python services (FastAPI, Django, Flask).1. InstallationThe package is designed to be installed via pip from the local source or internal registry.cd fusion-python
pip install .
2. The Interop Layer (Seamless JSON)The most critical component for integration with our React Frontend is the Interop class. It bridges the gap between Python's object-oriented cryptography and the Browser's JSON/JWK format.Accepting Keys from ReactWhen your React app sends engine.getPublicKeys(), it arrives as a JSON object.from fusion_core import HybridEngine, Interop

# 1. Initialize Engine (Global or Request-scoped)
engine = HybridEngine()
engine.init()

# 2. In your API Route
def handle_handshake(json_payload: dict):
    # Converts JSON -> HybridBundle
    # Validates JWK format and Matrix dimensions automatically
    client_keys = Interop.import_bundle(json_payload)
    
    # Proceed to encapsulation...
3. Performing the HandshakeOnce you have the HybridBundle, the process mirrors the TypeScript/Java implementations exactly.# 3. Encapsulate
# Returns the Session Key (keep secret!) and Ciphertext (send back)
session_key, raw_ciphertext = engine.encapsulate(client_keys)

(ecc_bytes, pqc_data) = raw_ciphertext

# 4. Format for React
# Converts the raw bytes back into the JSON format React expects
response = Interop.export_ciphertext(ecc_bytes, pqc_data)

return response # Return this as JSON
4. TelemetryThe Python package automatically logs performance metrics to stdout.[Telemetry] ➤ Hybrid Profiling: Fusion:Encapsulate
[Telemetry] ℹ Metrics: ECC=4.20ms | Lattice=1.50ms | Delta=-2.70ms
To capture these logs for a system like Datadog or Prometheus, override the Telemetry class methods.5. Security & GuardrailsJust like the other ecosystems, Python enforces the 50/50 standard.Guard.validate_hybrid_structure: Called automatically inside encapsulate.Type Safety: All inputs are strictly typed using Python 3.10+ type hints.