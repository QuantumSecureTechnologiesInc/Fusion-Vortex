[package]
name = "termblink-gpu"
version = "1.0.0"
edition = "2021"
description = "A production-grade headless-chrome to terminal bridge with WebGPU support"

[dependencies]
# Async Runtime
tokio = { version = "1.32", features = ["full"] }
futures = "0.3"

# Browser Automation (CDP)
chromiumoxide = { version = "0.5", features = ["tokio-runtime"] }

# Image Processing (Pinned for stability)
image = "0.24"

# Terminal UI & Input
crossterm = "0.27"

# Error Handling
anyhow = "1.0"

# CLI Argument Parsing
clap = { version = "4.4", features = ["derive"] }

# Logging (File-based, non-blocking)
tracing = "0.1"
tracing-subscriber = "0.3"
tracing-appender = "0.2"

# Directory management
directories = "5.0"


