[package]
name = "fusion_sec_secrets_auditor"
version = "0.1.0"
edition = "2021"
description = "Static analysis tool for detecting leaked credentials and secrets in source code"
license = "MIT"

[dependencies]
fusion_core = { path = "../core", version = "0.1.0" }