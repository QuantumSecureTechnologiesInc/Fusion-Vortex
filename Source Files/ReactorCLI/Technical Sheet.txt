Technical Sheet: ReactorCLISystem RequirementsOS: Cross-platform (Linux, Windows, macOS, WASI).Terminal: ANSI-compatible terminal required for Interactive Mode.Memory: < 2MB footprint (extremely lightweight compared to full GUI frameworks).PerformanceParse Time: < 50µs for typical command lines.Startup Overhead: Zero (unless Interactive Mode triggers).Binary Size: Adds ~300KB to binary (mostly due to TUI capability).Dependenciesclap (core parsing logic, wrapped and extended).dialoguer / inquire (for interactive prompts).serde (for config/schema serialization).