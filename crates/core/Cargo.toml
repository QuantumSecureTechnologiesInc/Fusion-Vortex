[package]
name = "fusion-core"
version = "0.1.0"
edition = "2021"
description = "Fusion language core - Type system, IR, and compiler primitives"

[lib]
name = "fusion_core_compiler"
path = "src/lib.rs"

[dependencies]
