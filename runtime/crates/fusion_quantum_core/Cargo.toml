[package]
name = "fusion_quantum_core"
version = "0.2.0"
edition = "2021"
authors = ["Quantum Secure Technologies Inc."]
description = "Quantum circuit and simulation core for Fusion"
license = "MIT OR Apache-2.0"

[dependencies]
fusion_traits = { path = "../fusion_traits" }
fusion_tensor_core = { path = "../fusion_tensor_core" }
# Removed fusion_runtime_core dependency to avoid circular dependency
num-complex = "0.4"
parking_lot = "0.12"

[dev-dependencies]

[lib]
name = "fusion_quantum_core"
path = "src/lib.rs"
