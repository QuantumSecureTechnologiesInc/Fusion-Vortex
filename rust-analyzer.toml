# Rust Analyzer workspace configuration
# Enables the 'wasm' feature for the fuc crate so rust-analyzer
# can properly parse wasm/codegen.rs and wasm/types.rs
# (these modules are behind #[cfg(feature = "wasm")])

[cargo]
features = ["wasm"]

[check]
allTargets = true
features = ["wasm"]
