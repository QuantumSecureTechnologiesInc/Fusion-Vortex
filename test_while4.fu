extern fn printf(fmt: string, ...) -> int;

fn test4(a: int, b: int, c: int, d: int) -> int {
    printf("call: %d\n", a);
    return 0;
}

fn main() -> int {
    printf("Start\n");
    let mut i: int = 0;
    while i < 3 {
        let r = test4(i, 1, 2, 3);
        i = i + 1;
    }
    printf("End\n");
    return 0;
}
