# fusion.toml - HAFT Project Configuration

[package]
name = "haft-fusion"
version = "0.1.0"
edition = "2.0"
authors = ["HAFT Architect"]
description = "Hyper-Adaptive Flux Tensor with Agentic Core"

[dependencies]
# Core Fusion stdlib includes async runtime, collections, and serialisation
# No external dependencies required - Fusion provides:
# - Native async/await runtime
# - shared<T> for thread-safe concurrency
# - Built-in serialisation
# - UUID generation via std::uuid

[features]
default = ["simd"]
simd = []  # Enable SIMD optimisations for DimensionalBarrier

[lib]
path = "src/lib.fu"

[[bin]]
name = "haft-fusion"
path = "src/main.fu"

# Note: In production, the DimensionalBarrier could be extended with
# external compression libraries via Fusion's FFI system