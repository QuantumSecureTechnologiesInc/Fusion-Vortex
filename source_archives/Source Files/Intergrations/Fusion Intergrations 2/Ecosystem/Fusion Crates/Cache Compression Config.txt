[package]
name = "fusion_llm_cache_compression"
version = "0.1.0"
edition = "2021"
description = "Algorithms for quantizing the Key-Value (KV) cache to save VRAM"
license = "MIT"

[dependencies]
fusion_core = { path = "../core", version = "0.1.0" }
fusion_llm_quantization = { path = "../llm-quantization", version = "0.1.0" }