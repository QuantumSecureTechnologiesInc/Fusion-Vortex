[package]
name = "fusion_llm_logits_processor"
version = "0.1.0"
edition = "2021"
description = "Post-processing chain for raw model logits (repetition penalty, output bias)"
license = "MIT"

[dependencies]
fusion_core = { path = "../core", version = "0.1.0" }