# A Hybrid App Manifest
[package]
name = "hybrid-engine"
version = "1.0.0"
language = "rust" # The main entry point is Rust

# Main Application Dependencies
[dependencies]
tokio = { version = "1.0", features = ["full"] }
fusion-rt = "3.0"

# C++ Physics Module
[languages.cpp]
standard = "c++20"
sources = ["src/native/physics.cpp", "src/native/sim.cpp"]
include_dirs = ["include/"]

# Python AI Module
[languages.python]
requirements = ["numpy", "pytorch"]
entry_point = "src/ai/model.py"

# JS Dashboard
[languages.js]
manager = "bun"
packages = { react = "^18.0", vite = "^4.0" }