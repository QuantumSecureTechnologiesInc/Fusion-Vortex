[package]
name = "fusion_security"
version = "0.1.0"
edition = "2021"
description = "Post-Quantum Cryptography (PQC) and Security implementation for Fusion"
license = "MIT"

[dependencies]
fusion_core = { path = "../core", version = "0.1.0" }
fusion_net = { path = "../net", version = "0.1.0" }
fusion_std = { path = "../std", version = "0.1.0" }

# PQC Primitives (Using Rust bindings to PQClean/NIST reference)
# Note: In a real environment, we'd use 'pqcrypto-kyber' and 'pqcrypto-dilithium'.
# For this simulation, we will assume these crates exist or mock the FFI if needed.
# We will use 'rand' and 'sha2' for the hybrid construction logic.
rand = "0.8"
sha2 = "0.10"
base64 = "0.21"
thiserror = "1.0"
async-trait = "0.1"