Reference: Fusion Standard Library1. PreludeThe fusion_std::prelude exports everything needed for a standard application.use fusion_std::prelude::*;
2. Modulesfs (Filesystem)Async, reactor-backed filesystem operations.File::open(path) -> Result<File>File::read(len) -> Result<Vec<u8>>net (Networking)Secure by Default. All streams are hybrid PQC encrypted.SecureTcpStream::connect(addr) -> Result<Self>SecureTcpListener::bind(addr) -> Result<Self>crypto (Security)Identity::generate(): Creates a Hybrid (X25519 + Kyber-768) identity.SecureChannel: Manages ChaCha20-Poly1305 sessions.compute (Heterogeneous)Fusion::spawn(future): Spawns a task on the reactor.vec.to_gpu(): Moves data to device memory.