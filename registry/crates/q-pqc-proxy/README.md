# PQC Proxy

A proxy layer for deciding where to execute Parameterized Quantum Circuits (PQC) - on simulator, local GPU, or cloud QPU.

## Features

- Latency-aware routing
- Cost optimization
- Backend abstraction

## Usage

Used internally by the Fusion runtime to route quantum jobs.