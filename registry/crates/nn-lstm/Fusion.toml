[package]
name = "nn-lstm"
version = "0.2.0"
edition = "2021"
authors = ["Fusion Team"]
license = "MIT"
description = "Algorithm: Long Short-Term Memory (LSTM) with O(4·d²) gate operations per timestep."
repository = "https://github.com/QuantumSecureTechnologiesInc/Fusion-Programming-Language"
keywords = ["lstm", "rnn", "neural-network", "algorithm"]
categories = ["algorithms", "science"]
readme = "README.md"

[dependencies]
fusion_core = { workspace = true }
fusion_ai_core = { workspace = true }
fusion_std = "1.0.0"

[dev-dependencies]
criterion = { workspace = true }

# [[bench]]
# name = "lstm_forward"
# harness = false
