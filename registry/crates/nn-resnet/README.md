# nn-resnet

Residual Network (ResNet) implementations.

## Installation

```toml
[dependencies]
nn-resnet = "0.1.0"
```text

## Features

- ResNet blocks
- Pre-built ResNet architectures (18, 50, 101)
- Skip connections