[package]
name = "fusion_api_rate_limiter"
version = "0.2.0"
edition = "2021"
description = "Distributed rate limiting (Token Bucket/Sliding Window) service"
license = "MIT"

[dependencies]
fusion_net = { path = "../fusion_net/", version = "0.2.0" }
fusion_std = { path = "../std", version = "0.2.0" }
tokio = { workspace = true }
thiserror = { workspace = true }
