[package]
name = "fusion_llm_batch_scheduler"
version = "0.2.0"
edition = "2021"
description = "Scheduler for dynamic batching and continuous batching during inference"
license = "MIT"

[dependencies]
fusion_core.workspace = true
fusion_llm_inference_engine = { path = "../llm-inference/", version = "0.2.0" }
fusion_net = { path = "../fusion_net/", version = "0.2.0" }
fusion_runtime_core = { workspace = true }
