[package]
name = "fusion_experimental_diagnostics"
version = "0.2.0"
edition = "2021"
description = "Diagnostics and load balancing tools for Mixture-of-Experts models"
license = "MIT"

[dependencies]
fusion_core = { workspace = true }
fusion_net = { path = "../fusion_net/", version = "0.2.0" }

