# CUDA Kernels

A collection of optimized CUDA kernels for high-performance computing tasks in Fusion.

## Overview

This crate contains pre-compiled PTX or source code for kernels used by `cuda-interface` and other accelerator crates.

## kernels

- Matrix multiplication
- Vector operations
- Quantum simulation primitives

## Usage

Used primarily as a dependency for higher-level crates.