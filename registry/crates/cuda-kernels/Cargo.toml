[package]
name = "fusion_llm_cuda_kernel_lib"
version = "0.2.0"
edition = "2021"
description = "FFI binding library for highly optimized CUDA kernels (Attention, RMSNorm, MatMul)"
license = "MIT"

[dependencies]
fusion_core = { workspace = true }
fusion-ai-core = { workspace = true }
# In prod: cuda-sys, cutlass FFI

