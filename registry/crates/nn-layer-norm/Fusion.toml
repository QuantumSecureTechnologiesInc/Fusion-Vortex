[package]
name = "nn-layer-norm"
version = "0.2.0"
edition = "2021"
authors = ["Fusion Team"]
license = "MIT"
description = "Algorithm: Layer normalization with O(n) complexity. Stabilizes transformer training."
repository = "https://github.com/QuantumSecureTechnologiesInc/Fusion-Programming-Language"
keywords = ["layer-norm", "normalization", "transformer", "neural-network", "algorithm"]
categories = ["algorithms", "science"]
readme = "README.md"

[dependencies]
fusion_core = { workspace = true }
fusion_ai_core = { workspace = true }
fusion_std = "1.0.0"

[dev-dependencies]
criterion = { workspace = true }

# [[bench]]
# name = "layer_norm_benchmark"
# harness = false
