# Component Library

This crate provides a collection of reusable components for the Fusion programming language ecosystem.

## Features
- Modular component design
- Reusable UI and logic elements
- Standardized component interfaces

## Usage
Add this to your `Cargo.toml`:
```toml
[dependencies]
component-lib = { path = "registry/crates/component-lib" }
```
