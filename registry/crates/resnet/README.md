# ResNet Implementation

Residual Network (ResNet) architecture implementation in Fusion.

## Features
- ResNet-18, 34, 50, 101, 152 models
- Pre-trained weights loading
- Training and inference utilities

## Usage
```rust
use resnet::ResNet50;

let model = ResNet50::new();
```
