[package]
name = "fusion_llm_inference_engine"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Algorithm: Large Language Model component: llm-inference. Optimized for performance."
authors = ["Fusion Team"]
repository = "https://github.com/QuantumSecureTechnologiesInc/Fusion-Programming-Language"
keywords = ["algorithm", "fusion", "llm", "generative-ai"]
categories = ["algorithms", "science"]
readme = "README.md"

[dependencies]
fusion_core = { path = "../fusion-core", package = "fusion-core" }
fusion_std = { path = "../std" }

[dev-dependencies]
criterion = { version = "0.5.1" }
