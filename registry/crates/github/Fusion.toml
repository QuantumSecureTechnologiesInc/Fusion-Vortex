[package]
name = "github"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Integration: GitHub API integration and workflow automation."
authors = ["Fusion Team"]
repository = "https://github.com/QuantumSecureTechnologiesInc/Fusion-Programming-Language"
keywords = ["fusion", "integration", "github", "api", "integration"]
categories = ["external-ffi-bindings", "network-programming"]
readme = "README.md"

[dependencies]
fusion_core = { path = "../fusion-core", package = "fusion-core" }
anyhow = { version = "1.0.94" }
thiserror = { version = "2.0.6" }
serde = { version = "1.0.216", features = ["derive", "rc"] }
serde_json = { version = "1.0.133" }
reqwest = { version = "0.12.9", features = ["json", "stream", "multipart"] }
fusion_runtime_core = { path = "../fusion_runtime_core", version = "0.2.0" }
chrono = { version = "0.4.39", features = ["serde"] }
fusion_std = { path = "../std" }

