[package]
name = "fusion_tokenizers"
version = "0.2.0"
edition = "2021"
description = "Production-grade BPE, SentencePiece, and specialized tokenizers"
license = "MIT"

[dependencies]
fusion_std = { path = "../std", version = "0.2.0" }
# In prod: tokenizers = "0.15" (Rust library)