[package]
name = "fusion_llm_gpu_scheduler"
version = "0.2.0"
edition = "2021"
description = "GPU VRAM and Resource Scheduler for concurrent LLM serving"
license = "MIT"

[dependencies]
fusion_core = { workspace = true }
fusion_llm_inference_engine = { path = "../llm-inference/", version = "0.2.0" } # KV Cache references
tokio = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true }

