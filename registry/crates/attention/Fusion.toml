[package]
name = "fusion_attention"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Algorithm: Multi-head attention with O(n²·d) complexity. Optimized for transformer models."
authors = ["Fusion Team"]
repository = "https://github.com/QuantumSecureTechnologiesInc/Fusion-Programming-Language"
keywords = ["attention", "transformer", "algorithm", "machine-learning"]
categories = ["algorithms", "science"]
readme = "README.md"

[dependencies]
fusion_core = { workspace = true }
fusion_ai_core = { workspace = true }
futures = { workspace = true }
fusion_std = "1.0.0"

[dev-dependencies]
criterion = { workspace = true }

# [[bench]]
# name = "attention_benchmark"
# harness = false

