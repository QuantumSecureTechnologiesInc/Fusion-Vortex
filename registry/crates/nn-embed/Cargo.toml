[package]
name = "fusion_nn_embed"
version = "0.2.0"
edition = "2021"
description = "Token and Positional Embeddings for sequence models"
license = "MIT"

[dependencies]
fusion_core = { workspace = true }
fusion_ai_core = { workspace = true }
fusion_ai_core_adapters = { workspace = true }

