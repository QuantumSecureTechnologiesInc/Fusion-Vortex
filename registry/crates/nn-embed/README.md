# nn-embed

Embedding layers for neural networks in Fusion.

## Installation

```toml
[dependencies]
nn-embed = "0.1.0"
```text

## Features

- Word embeddings
- Position embeddings
- Sparse and dense lookup