# Crate Analyzer

A tool for analyzing Rust crates within the Fusion ecosystem, providing insights into dependencies, structure, and metrics.

## Features

- Dependency graph analysis
- Code metric collection
- Structural validation

## Usage

```rust
use crate_analyzer::Analyzer;

let analysis = Analyzer::new().analyze("path/to/crate");
```text