# React Hooks (Fusion Bridge)

Rust bindings and utilities for interacting with React hooks in Fusion's web runtime.

## Features

- `use_state` wrapper
- `use_effect` wrapper
- Component lifecycle management

## Usage

Used within Fusion's Yew/React interoperability layer.