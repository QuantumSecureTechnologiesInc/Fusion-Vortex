# Embeddings

A library for generating and managing vector embeddings for AI models in Fusion.

## Features
- Interface for various embedding models
- Similarity search utilities
- Vector storage abstractions

## Usage
```rust
use embeddings::Embedder;

let vector = embedder.embed("text to embed")?;
```
