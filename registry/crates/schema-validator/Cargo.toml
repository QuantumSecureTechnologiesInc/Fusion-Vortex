[package]
name = "fusion_api_schema_validator"
version = "0.2.0"
edition = "2021"
description = "Runtime validation of JSON data against JSON Schema and OpenAPI specifications"
license = "MIT"

[dependencies]
fusion_core = { workspace = true }
fusion_std = { path = "../std", version = "0.2.0" }
serde_json = { workspace = true }
# In prod: jsonschema, valico

