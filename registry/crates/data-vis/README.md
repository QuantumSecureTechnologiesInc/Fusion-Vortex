# Data Visualization

A library for creating data visualizations in Fusion, supporting various chart types and rendering backends.

## Features
- 2D and 3D plotting
- Interactive charts
- Export to SVG, PNG, and terminal

## Usage
```rust
use data_vis::Chart;

let chart = Chart::new().data(points).render();
```
