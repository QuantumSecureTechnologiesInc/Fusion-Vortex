[package]
name = "fusion_llm_quantization"
version = "0.1.0"
edition = "2021"
description = "INT8/INT4 quantization for LLM weights and KV cache"
license = "MIT"

[dependencies]
fusion-core = { workspace = true }
fusion_std = "1.0.0"

