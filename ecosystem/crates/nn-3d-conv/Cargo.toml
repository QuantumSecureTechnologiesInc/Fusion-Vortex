[package]
name = "fusion_nn_3d_conv"
version = "0.1.0"
edition = "2021"
description = "3D Convolutional Layers for video and volumetric data (C * D * H * W)"
license = "MIT"

[dependencies]
fusion_core = { path = "../../../crates/core", version = "0.1.0" }
fusion_ai_core = { path = "../ai-core", version = "0.1.0" }
