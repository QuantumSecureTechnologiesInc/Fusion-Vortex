[package]
name = "fusion_llm_data_tokenizer"
version = "0.1.0"
edition = "2021"
description = "High-throughput, parallel data tokenization for training pipelines"
license = "MIT"

[dependencies]
fusion_std = { path = "../../../crates/core", version = "0.1.0" }
fusion_llm_tokenizers = { path = "../llm-tokenizers", version = "0.1.0" }
tokio = { workspace = true }
