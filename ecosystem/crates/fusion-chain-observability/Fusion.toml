[package]
name = "fusion-chain-observability"
version = "0.1.0"
edition = "2021"
authors = ["Fusion Team"]
description = "Operational telemetry for chain health"
license = "MIT"

[dependencies]
fusion_std = "1.0.0"
