[package]
name = "llm-lora-kernel"
version = "0.1.0"
edition = "2021"
description = "Fused kernel for base weight + LORA adapter multiplication (W + AB)"
license = "MIT"

[dependencies]
fusion-core = { workspace = true }
fusion-ai-core = { workspace = true }
fusion_std = "1.0.0"


