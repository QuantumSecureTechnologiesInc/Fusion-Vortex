[package]
name = "fusion_llm_lora_kernel"
version = "0.1.0"
edition = "2021"
description = "Fused kernel for base weight + LORA adapter multiplication (W + AB)"
license = "MIT"

[dependencies]
fusion_core = { path = "../../../crates/core", version = "0.1.0" }
fusion_ai_core = { path = "../ai-core", version = "0.1.0" }
