[package]
name = "fusion_llm_lora_manager"
version = "0.1.0"
edition = "2021"
description = "LoRA adapter manager for LLM models"
license = "MIT"

[dependencies]
fusion-core = { workspace = true }
fusion_std = "1.0.0"

