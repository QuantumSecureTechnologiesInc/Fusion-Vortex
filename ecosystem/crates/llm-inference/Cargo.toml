[package]
name = "fusion_llm_inference_engine"
version = "0.1.0"
edition = "2021"
description = "LLM inference engine for running models"
license = "MIT"

[dependencies]
fusion-core = { path = "../../../crates/core", version = "0.1.0" }
