[package]
name = "llm-inference"
version = "0.1.0"
edition = "2021"
description = "LLM inference engine for running models"
license = "MIT"

[dependencies]
fusion-core = { workspace = true }
fusion_std = "1.0.0"


