[package]
name = "nn-metrics"
version = "0.1.0"
edition = "2021"
description = "Training metrics calculation (Accuracy, F1, AUC, etc.)"
license = "MIT"

[dependencies]
fusion-core = { workspace = true }
fusion_std = "1.0.0"


