[package]
name = "fusion-chain-ledger"
version = "0.1.0"
edition = "2021"
authors = ["Fusion Team"]
description = "Append-only deterministic ledger"
license = "MIT"

[dependencies]
fusion_std = "1.0.0"
fusion-chain-primitives = { path = "../fusion-chain-primitives" }
