[package]
name = "llm-logits-processor"
version = "0.1.0"
edition = "2021"
description = "Post-processing chain for raw model logits (repetition penalty, output bias)"
license = "MIT"

[dependencies]
fusion-core = { workspace = true }
fusion_std = "1.0.0"


