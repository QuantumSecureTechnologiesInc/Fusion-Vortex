[package]
name = "fusion_nn_gan_layers"
version = "0.1.0"
edition = "2021"
description = "Generative layers (Transpose Conv, Pixel Shuffler) "
license = "MIT"

[dependencies]
fusion-core = { path = "../../../crates/core", version = "0.1.0" }
fusion-ai-core = { path = "../ai-core", version = "0.1.0" }
